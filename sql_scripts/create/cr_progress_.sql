# create

CREATE TABLE `bearberry`.`progress_` (
  `id` INT NOT NULL,
  `eid` INT NOT NULL,
  `week` INT NOT NULL,
  `weight` FLOAT NOT NULL);

SELECT * FROM `bearberry`.`progress_`;

DROP TABLE `bearberry`.`progress_`;